# 納瓦爾工作坊 — 接手須知（新 Claude Code session 先讀這段）

**第一件事**：讀同目錄 `00-交接包-給接手Session.md`（全狀態／檔案位置／要改哪段／坑）。Claude 的本機記憶不跨機器，那份＝記憶替代。

## 鐵規
- 受眾＝**扛霸子**（保險經紀團隊領導）。
- **禁用**「竹孟／築夢／修哥」；讀書法段用本名「孟修」。
- 語言：繁體中文（台灣用語）。

## 這個 repo 是什麼
- `index.html` ＝ 線上站本體（11 分頁）。**改 index.html → `git push` 就自動重新部署**到 <https://lawsuger.github.io/naval-workshop/>（約 1 分鐘）。
- `opening/` 去品牌動畫、`webapp/` 互動工作簿。
- 要改特定東西的位置（`ADVISOR`/`D[]`/`QZ[]`/`PAIN{}`/`LIB[]`/配色 `:root`）見交接包 §4。

## 完整素材（PPTX／MD／表單／Vol-01）
不在本 repo，在 Obsidian vault（Google Drive 同步）：`…/大腦資料庫/outputs/readings/2026-06-08-…處經理工作坊/`。